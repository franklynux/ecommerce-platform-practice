{
  "name": "pet-accessories-ecommerce",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install",
    "start": "docker compose up",
    "dev": "docker compose -f docker-compose.dev.yml up",
    "build": "docker compose build"
  },
  "devDependencies": {
    "postcss-preset-env": "^10.1.0"
  }
}
